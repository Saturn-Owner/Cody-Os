[project]
name = "codyos-hermes-integration"
version = "0.1.0a0"
description = "Generische öffentliche CodyOS-Integration für nutzereigene Hermes-API-Backends"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27"]

[tool.setuptools.packages.find]
where = ["."]
include = ["codyos_hermes_integration*"]
