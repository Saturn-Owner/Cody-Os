[project]
name = "codyos-hermes-integration"
version = "0.1.0"
description = "Generic public CodyOS integration for user-owned Hermes API backends"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27"]

[tool.setuptools.packages.find]
where = ["."]
include = ["codyos_hermes_integration*"]
