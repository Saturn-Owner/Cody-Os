org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# Gateway endpoint — these are safe placeholder values (see GatewayConfig.kt).
# Point them at your own Gateway. If you're contributing back, please don't
# commit your own real URLs here — keep this file at the placeholder values
# in commits, and let your local uncommitted edit hold your real ones.
CODY_HOME_HTTPS_URL=https://your-domain.example/cody-home
CODY_HOME_WSS_URL=wss://your-domain.example/cody-home/ws
